# Installed by: pip install -r requirements.txt
#
# Only direct dependencies are listed here; pip resolves the rest. The agent in
# these codelabs runs on the Conductor server through an Orkes integration, so
# the optional 'conductor-python[agents]' extra (which pulls in LangChain,
# LangGraph, the OpenAI and Anthropic SDKs, and Google ADK) is not required.
conductor-python==2.0.0
python-dotenv==1.2.3
requests==2.34.2
